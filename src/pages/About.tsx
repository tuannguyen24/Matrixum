import React, { FC } from 'react';
import { Header } from '../components/organisms/Header';

const About: FC = () => {
  return (
    <React.Fragment>
      <Header />
    </React.Fragment>
  );
};

export default About;

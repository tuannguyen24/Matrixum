import React, { FC } from 'react';
import { Header } from '../components/organisms/Header';

const Home: FC = (props) => {
  return (
    <React.Fragment>
      <Header />
    </React.Fragment>
  );
};

export default Home;
